{
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "http://127.0.0.1:8000/github-mcp",
      "headers": {
        "Authorization": "Bearer ${GITHUB_TOKEN}",
        "apikey": "${KONG_CLIENT_KEY}"
      }
    }
  }
}
